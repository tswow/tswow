export declare class DBCBuffer {
    private rows;
    private strings;
    private bufPtr;
    private strPtr;
    private deletedIndices;
    private _rowSize;
    private _rowCount;
    private _baseRowCount;
    private _fieldCount;
    static getBuffer(file: DBCBuffer): Buffer;
    get rowCount(): number;
    get baseRowCount(): number;
    get rowSize(): number;
    protected deleteIndex(search: number): number;
    isDeleted(rowNo: number): boolean;
    undelete(rowNo: number): void;
    delete(rowNo: number): void;
    initialize(rowSize: number): void;
    applyDeletes(): void;
    write(): Buffer;
    private sliceRows;
    move(sourceIndex: number, targetIndex: number): void;
    swap(index1: number, index2: number): void;
    read(file: string): void;
    readstring(offset: number): string;
    writebool(value: boolean, offset: number): void;
    readbool(offset: number): boolean;
    writestring(str: string, offset: number): void;
    getOffset(rowNo: number): number;
    addRow(sourceLine: number): number;
    readint(offset: number): number;
    readbyte(offset: number): number;
    writebyte(value: number, offset: number): void;
    writeint(value: number, offset: number): void;
    readuint(offset: number): number;
    writeuint(value: number, offset: number): void;
    writeulong(value: bigint, offset: number): void;
    readulong(offset: number): bigint;
    writefloat(value: number, offset: number): void;
    readfloat(offset: number): number;
}
